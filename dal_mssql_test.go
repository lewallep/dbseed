package dbseed

import (
	"fmt"
	"testing"
)

// Might not be needed since I am pulling the column names and types directly from the
// database after they are created.
func TestColumnCreation(t *testing.T) {

}